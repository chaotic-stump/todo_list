/ <div class="six columns">
%h1 Items

- @items.each do |item|
  = "#{item.name} - #{item.description} - #{item.complete}"
  =link_to 'Show', item_path(item), :class => 'button button-primary'
  =link_to 'Edit', edit_item_path(item), :class => 'button button-warning'
  =link_to 'Delete', item, :class => 'button button-danger', method: :delete
  %br
%p= link_to 'Add Item', new_item_path, :class => 'button button-success'
/ </div>
-# TODO conditional hide separator if there is no description
-# TODO replace buttons with function icons on the same line.
