%h1 Edit Item
= form_for @item do |f|
  = f.label :name
  = f.text_field :name
  %br/
  = f.label :description
  = f.text_field :description
  %br/
  = f.check_box :complete
  = f.submit 'Update item', :class => "button button-primary"
